---
phase: 01-baseline-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .planning/phases/01-baseline-verification/01-BASELINE.md
  - .planning/phases/01-baseline-verification/01-01-PLAN.md
  - .planning/phases/01-baseline-verification/01-evidence-prohibitions.test.cjs
  - .planning/phases/01-baseline-verification/fixtures/01-BASELINE-violation.md
autonomous: true
requirements:
  - BASE-01
  - BASE-02
gap_closure: true
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "BASE-01: ADMIN-01 cites existing credential implementation and test files."
    - "BASE-01: MODEL-01 through MODEL-05 identify `_kotlin/model-server` as Python 3.13 and never use it as Kotlin PASS evidence."
    - "BASE-02 and D-06: Current Kotlin timestamp/cutoff implementation is STATIC_MATCH and its current tests are PASS, while the unidentified remote artifact and live reproduction remain NOT_RUN."
    - "The evidence precheck rejects missing Kotlin paths, inaccurate MODEL or D-06 rows, empty evidence cells, missing requirement IDs, and missing active routes before prohibition metadata changes."
    - "The original two prohibitions use GSD's canonical node-test descriptor and become green only after fail-first proof and a non-vacuous clean run."
  artifacts:
    - path: .planning/phases/01-baseline-verification/01-BASELINE.md
      provides: Corrected implementation paths, language classification, states, and D-06 evidence separation
    - path: .planning/phases/01-baseline-verification/01-01-PLAN.md
      provides: Resolved test-tier prohibitions with canonical flat wired-check descriptors
    - path: .planning/phases/01-baseline-verification/01-evidence-prohibitions.test.cjs
      provides: Node test that enforces current-PASS semantics and all six active routes
    - path: .planning/phases/01-baseline-verification/fixtures/01-BASELINE-violation.md
      provides: Known-bad subject that machine-proves the prohibition test fails first
  key_links:
    - from: _kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/service/AdminService.kt
      to: .planning/phases/01-baseline-verification/01-BASELINE.md
      via: ADMIN-01 implementation evidence
    - from: _kotlin/model-server/app/main.py
      to: .planning/phases/01-baseline-verification/01-BASELINE.md
      via: Python 3.13 MODEL-01 through MODEL-05 evidence
    - from: .planning/phases/01-baseline-verification/01-evidence-prohibitions.test.cjs
      to: .planning/phases/01-baseline-verification/01-01-PLAN.md
      via: check_target with violation and clean fixtures consumed by GSD prohibition-enforcement
---

<objective>
Correct the Phase 1 evidence ledger and replace the two unverified prohibition dispositions with deterministic test evidence.

Purpose: Close the two evidence-accuracy gaps in `01-VERIFICATION.md` without changing product code or deferred work.
Output: Corrected `01-BASELINE.md`, a fail-first prohibition test and fixture, and canonical wired prohibition metadata in `01-01-PLAN.md`.
</objective>

<execution_context>
@/home/wooclee/.codex/gsd-core/workflows/execute-plan.md
@/home/wooclee/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-baseline-verification/01-VERIFICATION.md
@.planning/phases/01-baseline-verification/01-BASELINE.md
@.planning/phases/01-baseline-verification/01-01-PLAN.md
@.planning/phases/01-baseline-verification/01-01-SUMMARY.md
@.planning/phases/01-baseline-verification/01-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct Phase 1 evidence and wire fail-closed prohibition checks</name>
  <read_first>
    .planning/phases/01-baseline-verification/01-VERIFICATION.md
    .planning/phases/01-baseline-verification/01-BASELINE.md
    .planning/phases/01-baseline-verification/01-01-PLAN.md
    .planning/phases/01-baseline-verification/01-CONTEXT.md
    .planning/REQUIREMENTS.md
    _kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/service/AdminService.kt
    _kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/security/CredentialCipher.kt
    _kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/api/AdminApiIntegrationTest.kt
    _kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/security/CredentialCipherTest.kt
    _kotlin/model-server/README.md
    _kotlin/model-server/Dockerfile
    _kotlin/model-server/app/main.py
    _kotlin/model-server/app/contracts.py
    _kotlin/model-server/app/config.py
    _kotlin/model-server/app/runtime.py
    _kotlin/model-server/tests/test_api.py
    _kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/ingestion/ConfluenceConnectorLoader.kt
    _kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/ingestion/IngestionWorker.kt
    _kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/ingestion/OpenSearchIndexer.kt
    _kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/ingestion/ConfluenceConnectorLoaderTest.kt
    _kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/ingestion/OpenSearchIndexerTest.kt
    _kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/ingestion/OpenSearchIndexerIntegrationTest.kt
  </read_first>
  <files>.planning/phases/01-baseline-verification/01-BASELINE.md, .planning/phases/01-baseline-verification/01-01-PLAN.md, .planning/phases/01-baseline-verification/01-evidence-prohibitions.test.cjs, .planning/phases/01-baseline-verification/fixtures/01-BASELINE-violation.md</files>
  <action>Correct ADMIN-01 to cite the full `AdminService.kt`, `CredentialCipher.kt`, `AdminApiIntegrationTest.kt`, and `CredentialCipherTest.kt` paths. Correct each MODEL row from the inspected Python 3.13 model-server files. MODEL-01 must cite `app/main.py`, `app/contracts.py`, `app/runtime.py`, and `tests/test_api.py`; MODEL-02 must cite `app/contracts.py`, `app/config.py`, `app/runtime.py`, and `tests/test_api.py`; MODEL-03 must cite `README.md`, `Dockerfile`, `app/config.py`, `app/runtime.py`, and `tests/test_api.py`; MODEL-04 must cite `app/runtime.py` and `tests/test_api.py`; MODEL-05 must cite `Dockerfile`, `app/runtime.py`, and `tests/test_api.py`. Use full `_kotlin/model-server/...` paths. Every MODEL row must state `Python 3.13` and `not Kotlin delivery evidence`. Set MODEL-01, MODEL-02, and MODEL-03 to exactly `STATIC_MATCH + GAP + NOT_RUN`. Set MODEL-04 and MODEL-05 to exactly `GAP + NOT_RUN`. Explain which Python behavior exists, which Kotlin delivery is missing, which coverage is absent, and which Python, golden, or benchmark run remains unexecuted.

Correct CONNECTOR-03 with the full current Kotlin loader, worker, indexer, and three test paths. Give its current executed-test status exactly `PASS`; describe the implementation path as STATIC_MATCH in its implementation text. Mark remote artifact identity and live reproduction as `REMOTE_EVALUATION NOT_RUN`, not as a current Kotlin gap. Add a `D-06 Evidence Separation` table whose `D-06 CURRENT_KOTLIN` row has separate `STATIC_MATCH` implementation and `PASS` current-verification cells. Its `D-06 REMOTE_EVALUATION` row has `NOT_RUN` identity and reproduction cells. Rewrite the active D-06 route to state that current Kotlin is STATIC_MATCH/PASS and unidentified remote evaluation is NOT_RUN. Preserve Phase 4 and Phase 5 routing. Do not describe the unidentified observation as a current Kotlin GAP. Preserve all six active routes.

Create `01-evidence-prohibitions.test.cjs` with Node's built-in `node:test`, `node:assert/strict`, and `node:fs`. Read `GSD_PROHIB_SUBJECT` when set. Otherwise, read `01-BASELINE.md`. Add one named test that rejects a requirement row when its current PASS status also carries historical, static, unrun, or environment-blocked result semantics. Add a second named test that requires D-06, D-07, D-08, D-09, D-13, and D-14 with their assigned phases. Keep both test names distinct from the test filename so GSD can prove a non-vacuous RED. Create `fixtures/01-BASELINE-violation.md` as a small known-bad subject with one mixed-semantics PASS row and missing active routes. The corrected `01-BASELINE.md` is the known-clean control.

Run the first automated evidence precheck before changing the prohibition metadata. After it passes, replace each original prohibition's fallback fields in `01-01-PLAN.md` with GSD's canonical flat contract: `status: "resolved"`, `verification: "test"`, `check_kind: "node-test"`, `check_target: ".planning/phases/01-baseline-verification/01-evidence-prohibitions.test.cjs"`, `check_violation_fixture: ".planning/phases/01-baseline-verification/fixtures/01-BASELINE-violation.md"`, and `check_clean_fixture: ".planning/phases/01-baseline-verification/01-BASELINE.md"`. Do not add a nested descriptor or a custom evidence scalar. Run the second automated check to prove both projected descriptors locate, fail on the violation fixture, pass on the clean control, and produce green test-tier dispositions. Commit all four evidence artifact changes once. Do not edit product code, root Python, root Web, `_kotlin/web`, the watchlist, or historical `01-01-SUMMARY.md`.</action>
  <verify>
    <automated>bash -c 'set -euo pipefail; f=.planning/phases/01-baseline-verification/01-BASELINE.md; req=.planning/REQUIREMENTS.md; ids=$(mktemp); rows=$(mktemp); refs=$(mktemp); paths=$(mktemp); trap '\''rm -f "$ids" "$rows" "$refs" "$paths"'\'' EXIT; rg -o '\''\*\*[A-Z]+-[0-9][0-9]\*\*'\'' "$req" | tr -d '\''*'\'' | sort -u > "$ids"; awk -F"|" '\''$2 ~ /^[[:space:]]*(BASE|ADMIN|PUBLIC|MODEL|INDEX|BUILD|INGEST|CONNECTOR|SEARCH|MCP)-[0-9][0-9][[:space:]]*$/ { id=$2; gsub(/^[[:space:]]+|[[:space:]]+$/, "", id); print id }'\'' "$f" | sort -u > "$rows"; test "$(wc -l < "$ids")" -eq 35; test "$(wc -l < "$rows")" -eq 35; cmp -s "$ids" "$rows"; awk -F"|" '\''$2 ~ /^[[:space:]]*(BASE|ADMIN|PUBLIC|MODEL|INDEX|BUILD|INGEST|CONNECTOR|SEARCH|MCP)-[0-9][0-9][[:space:]]*$/ { count++; for (i=2; i!=10; i++) { cell=$i; gsub(/^[[:space:]]+|[[:space:]]+$/, "", cell); if (cell == "") invalid=1 } } END { if (count != 35 || invalid) exit 1 }'\'' "$f"; rg -o "\`[^\`]+\\.kt\`" "$f" | tr -d "\`" | sort -u > "$refs"; rg --files _kotlin/backend > "$paths"; while IFS= read -r ref; do case "$ref" in _kotlin/*) test -f "$ref" || exit 1 ;; *) count=$(awk -v suffix="/$ref" '\''index($0, suffix) > 0 && index($0, suffix) == length($0)-length(suffix)+1 { n++ } END { print n+0 }'\'' "$paths"); test "$count" -eq 1 || exit 1 ;; esac; done < "$refs"; require_row() { id=$1; shift; line=$(awk -F"|" -v id="$id" '\''$2 ~ "^[[:space:]]*" id "[[:space:]]*$" { print; n++ } END { if (n != 1) exit 1 }'\'' "$f"); for value in "$@"; do case "$line" in *"$value"*) ;; *) exit 1 ;; esac; done; }; require_row ADMIN-01 "_kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/service/AdminService.kt" "_kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/security/CredentialCipher.kt" "_kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/api/AdminApiIntegrationTest.kt" "_kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/security/CredentialCipherTest.kt"; admin_line=$(awk -F"|" '\''$2 ~ /^[[:space:]]*ADMIN-01[[:space:]]*$/ { print }'\'' "$f"); case "$admin_line" in *CredentialService.kt*) exit 1 ;; esac; awk -F"|" '\''BEGIN { expected["MODEL-01"]="STATIC_MATCH + GAP + NOT_RUN"; expected["MODEL-02"]="STATIC_MATCH + GAP + NOT_RUN"; expected["MODEL-03"]="STATIC_MATCH + GAP + NOT_RUN"; expected["MODEL-04"]="GAP + NOT_RUN"; expected["MODEL-05"]="GAP + NOT_RUN" } $2 ~ /^[[:space:]]*MODEL-0[1-5][[:space:]]*$/ { id=$2; state=$6; gsub(/^[[:space:]]+|[[:space:]]+$/, "", id); gsub(/^[[:space:]]+|[[:space:]]+$/, "", state); seen[id]++; if (state != expected[id] || $0 !~ /Python 3[.]13/ || $0 !~ /not Kotlin delivery evidence/) invalid=1 } END { for (id in expected) if (seen[id] != 1) invalid=1; if (invalid) exit 1 }'\'' "$f"; require_row MODEL-01 "_kotlin/model-server/app/main.py" "_kotlin/model-server/app/contracts.py" "_kotlin/model-server/app/runtime.py" "_kotlin/model-server/tests/test_api.py"; require_row MODEL-02 "_kotlin/model-server/app/contracts.py" "_kotlin/model-server/app/config.py" "_kotlin/model-server/app/runtime.py" "_kotlin/model-server/tests/test_api.py"; require_row MODEL-03 "_kotlin/model-server/README.md" "_kotlin/model-server/Dockerfile" "_kotlin/model-server/app/config.py" "_kotlin/model-server/app/runtime.py" "_kotlin/model-server/tests/test_api.py"; require_row MODEL-04 "_kotlin/model-server/app/runtime.py" "_kotlin/model-server/tests/test_api.py"; require_row MODEL-05 "_kotlin/model-server/Dockerfile" "_kotlin/model-server/app/runtime.py" "_kotlin/model-server/tests/test_api.py"; for path in _kotlin/model-server/README.md _kotlin/model-server/Dockerfile _kotlin/model-server/app/main.py _kotlin/model-server/app/contracts.py _kotlin/model-server/app/config.py _kotlin/model-server/app/runtime.py _kotlin/model-server/tests/test_api.py; do test -f "$path" || exit 1; done; require_row CONNECTOR-03 "_kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/ingestion/ConfluenceConnectorLoader.kt" "_kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/ingestion/IngestionWorker.kt" "_kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/ingestion/OpenSearchIndexer.kt" "_kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/ingestion/ConfluenceConnectorLoaderTest.kt" "_kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/ingestion/OpenSearchIndexerTest.kt" "_kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/ingestion/OpenSearchIndexerIntegrationTest.kt" "REMOTE_EVALUATION NOT_RUN"; awk -F"|" '\''$2 ~ /^[[:space:]]*CONNECTOR-03[[:space:]]*$/ { row++; state=$6; gsub(/^[[:space:]]+|[[:space:]]+$/, "", state); if (state != "PASS" || toupper($0) ~ /CURRENT KOTLIN GAP/ || tolower($0) ~ /evaluated deployment ignored/) invalid=1 } $2 ~ /^[[:space:]]*D-06 CURRENT_KOTLIN[[:space:]]*$/ { current++; impl=$3; run=$4; gsub(/^[[:space:]]+|[[:space:]]+$/, "", impl); gsub(/^[[:space:]]+|[[:space:]]+$/, "", run); if (impl != "STATIC_MATCH" || run != "PASS") invalid=1 } $2 ~ /^[[:space:]]*D-06 REMOTE_EVALUATION[[:space:]]*$/ { remote++; identity=$3; run=$4; gsub(/^[[:space:]]+|[[:space:]]+$/, "", identity); gsub(/^[[:space:]]+|[[:space:]]+$/, "", run); if (identity != "NOT_RUN" || run != "NOT_RUN") invalid=1 } $2 ~ /^[[:space:]]*D-06[[:space:]]*$/ { route++; if ($0 !~ /STATIC_MATCH/ || $0 !~ /PASS/ || $0 !~ /NOT_RUN/ || $0 !~ /Phase 4, Phase 5/ || toupper($0) ~ /CURRENT KOTLIN GAP/ || tolower($0) ~ /ignored/) invalid=1 } END { if (row != 1 || current != 1 || remote != 1 || route != 1 || invalid) exit 1 }'\'' "$f"; for route in "D-06 .* Phase 4, Phase 5" "D-07 .* Phase 4" "D-08 .* Phase 5" "D-09 .* Phase 5" "D-13 .* Phase 3" "D-14 .* Phase 2"; do rg -q "$route" "$f" || exit 1; done; node --test --test-reporter=tap -- .planning/phases/01-baseline-verification/01-evidence-prohibitions.test.cjs'</automated>
    <fails_when>The baseline lacks any of the 35 unique requirement IDs, has an empty evidence cell, cites a missing or ambiguous Kotlin file, has an inaccurate ADMIN, MODEL, CONNECTOR-03, or D-06 row, omits an active route, or the clean prohibition test is empty or fails.</fails_when>
    <automated>bash -c 'set -euo pipefail; p=.planning/phases/01-baseline-verification/01-01-PLAN.md; target=.planning/phases/01-baseline-verification/01-evidence-prohibitions.test.cjs; bad=.planning/phases/01-baseline-verification/fixtures/01-BASELINE-violation.md; clean=.planning/phases/01-baseline-verification/01-BASELINE.md; tool=/home/wooclee/.codex/gsd-core/bin/gsd-tools.cjs; region=$(mktemp); trap '\''rm -f "$region"'\'' EXIT; sed -n '\''/^  prohibitions:/,/^  artifacts:/p'\'' "$p" > "$region"; test "$(rg -c '\''status: "resolved"'\'' "$region")" -eq 2; test "$(rg -c '\''verification: "test"'\'' "$region")" -eq 2; test "$(rg -c '\''check_kind: "node-test"'\'' "$region")" -eq 2; test "$(rg -Fc "check_target: \"$target\"" "$region")" -eq 2; test "$(rg -Fc "check_violation_fixture: \"$bad\"" "$region")" -eq 2; test "$(rg -Fc "check_clean_fixture: \"$clean\"" "$region")" -eq 2; ! rg -q '\''^[[:space:]]+(flagged|evidence):'\'' "$region"; for statement in "The baseline MUST NOT label historical, static, unrun, or environment-blocked evidence as a current PASS." "The baseline MUST NOT omit a confirmed active gap or its assigned follow-up phase."; do rg -Fq "statement: \"$statement\"" "$region" || exit 1; request=$(node -e '\''const statement=process.argv[1],target=process.argv[2],bad=process.argv[3],clean=process.argv[4]; process.stdout.write(JSON.stringify({prohibition:{statement,status:"resolved",verification:"test"},check:{kind:"node-test",target,violationFixture:bad,cleanFixture:clean}}))'\'' "$statement" "$target" "$bad" "$clean"); result=$(node "$tool" check prohibition-enforcement --json "$request"); node -e '\''let r; try { r=JSON.parse(process.argv[1]) } catch { process.exit(1) } const e=Array.isArray(r.evidence) ? r.evidence[0] : null; if (r.status!=="green" || r.flagged!==false || r.located!==true || r.kind!=="node-test" || !e || e.passed!==true || e.failFirst!==true || e.failFirstProof!=="violation-fixture") process.exit(1)'\'' "$result"; done'</automated>
    <fails_when>Either prohibition lacks the canonical flat descriptor, retains fallback or custom evidence fields, cannot produce a content-dependent RED on the violation fixture, fails the clean control, or does not receive a green unflagged disposition from GSD prohibition-enforcement.</fails_when>
  </verify>
  <acceptance_criteria>
    - ADMIN-01 cites only existing full Kotlin paths and exact credential tests.
    - Each MODEL row cites its required Python 3.13 files and has its exact expected state without a Kotlin PASS claim.
    - CONNECTOR-03 and D-06 separate current Kotlin implementation and passing tests from unidentified remote NOT_RUN evidence.
    - The precheck proves 35-ID coverage, non-empty evidence cells, exact paths, states, and routes before metadata changes.
    - Both prohibitions carry flat node-test descriptors and receive green only through GSD's fail-closed producer.
    - One execution commit changes only the four listed Phase 1 evidence artifacts.
  </acceptance_criteria>
  <done>Both verification gaps are closed with repository-backed evidence and machine-proven, canonically wired test-tier prohibitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository files to evidence ledger | Documentation can cite missing paths or misclassify an implementation language. |
| Remote evaluation to current Kotlin evidence | An unidentified deployment observation can be mistaken for this checkout's behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | `01-BASELINE.md` references and statuses | high | mitigate | Resolve every Kotlin reference, enforce exact MODEL and D-06 states, and repeat the 35-ID and non-empty-cell audits. |
| T-01-05 | Repudiation | Original prohibition disposition | medium | mitigate | Wire a node-test target with known-bad and known-clean subjects through GSD prohibition-enforcement. |
| T-01-06 | Information Disclosure | Evidence correction | low | mitigate | Read paths and status summaries only; do not copy secrets or runtime environment values. |
</threat_model>

## Artifacts this phase produces

- `.planning/phases/01-baseline-verification/01-evidence-prohibitions.test.cjs` — canonical node-test enforcement target.
- `.planning/phases/01-baseline-verification/fixtures/01-BASELINE-violation.md` — known-bad fail-first fixture.
- `.planning/phases/01-baseline-verification/01-02-SUMMARY.md` — gap closure execution record.
- New product symbols: none.

## Multi-Source Coverage Audit

| Source type | Item | Coverage |
|-------------|------|----------|
| VERIFICATION | Missing ADMIN-01 path and inaccurate MODEL language/status | Task 1 corrects and validates both. |
| VERIFICATION | D-06 evidence conflation and two unverified prohibitions | Task 1 separates current and remote evidence and wires canonical fail-closed enforcement. |
| REQ | BASE-01 | Existing paths and correct model-server classification restore traceability. |
| REQ | BASE-02 | State-mixing and D-06 separation checks restore evidence fidelity. |
| CONTEXT | D-01 through D-03, D-10 through D-12 | Only Phase 1 evidence changes; root and product code remain reference-only. |
| CONTEXT | D-04 and D-05 | Deferred items remain excluded. |
| CONTEXT | D-06 through D-09, D-13, D-14 | Routes remain present; only D-06 evidence classification changes. |

Deferred product work in `01-VERIFICATION.md` remains assigned to Phases 2-5 and is not a Phase 1 gap task.

<verification>
Run the evidence precheck before changing prohibition metadata. Then run the separate GSD prohibition-enforcement check. The executor creates `01-02-SUMMARY.md`; it does not rewrite the historical `01-01-SUMMARY.md`.
</verification>

<success_criteria>
- The deterministic precheck confirms all 35 rows have non-empty cells and exact corrected evidence.
- No referenced Kotlin file is missing or ambiguous.
- Each MODEL row has its required Python paths and exact non-PASS state.
- CONNECTOR-03 and D-06 do not classify unidentified remote evidence as a current Kotlin gap.
- GSD machine-proves RED on the violation fixture, GREEN on the clean control, and green dispositions for both prohibitions.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-verification/01-02-SUMMARY.md` when done.
</output>
