---
phase: 02-public-document-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/service/FileStorageService.kt
  - _kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/api/AdminApiIntegrationTest.kt
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - PUBLIC-01
  - PUBLIC-02
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "A failed upload leaves no physical file or file_assets row created by that request."
    - "A committed upload keeps its physical files, including ZIP entries and metadata."
    - "Existing ADMIN-01 through ADMIN-03 and PUBLIC-01 through PUBLIC-02 behavior remains unchanged."
  artifacts:
    - path: "_kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/service/FileStorageService.kt"
      provides: "Shared transaction rollback cleanup for newly stored file assets"
      contains: "TransactionSynchronizationManager"
    - path: "_kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/api/AdminApiIntegrationTest.kt"
      provides: "Request-level rollback proof across metadata, a completed ZIP entry, and a partial oversized entry"
      contains: "failedZipUploadRemovesFilesCreatedByTheRolledBackRequest"
  key_links:
    - from: "FileStorageService.store and FileStorageService.storeZipEntry"
      to: "the shared FileAssetRepository save boundary"
      via: "both new paths use the same rollback-registration helper"
      pattern: "TransactionSynchronizationManager\\.registerSynchronization"
    - from: "AdminApiIntegrationTest.failedZipUploadRemovesFilesCreatedByTheRolledBackRequest"
      to: "POST /manage/admin/connector/file/upload"
      via: "MockMvc multipart request through the real transactional service"
      pattern: "connector/file/upload"
---

<objective>
Close D-14 at the shared Kotlin file-save boundary without changing existing admin or public-document contracts.

Purpose: Keep physical storage consistent with the database when an upload transaction rolls back.
Output: One service fix, one focused integration test, and one consolidated product/test/summary commit.
</objective>

<execution_context>
@/home/wooclee/.codex/gsd-core/workflows/execute-plan.md
@/home/wooclee/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-baseline-verification/01-BASELINE.md
@.planning/phases/01-baseline-verification/01-VERIFICATION.md
@.planning/phases/02-public-document-management/02-CONTEXT.md
@.planning/phases/02-public-document-management/02-RESEARCH.md
@.planning/phases/02-public-document-management/02-PATTERNS.md
@_kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/service/FileStorageService.kt
@_kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/api/AdminApiIntegrationTest.kt

<interfaces>
`FileStorageService.upload` and `updateConnectorFiles` are the existing `@Transactional` entry points.
`store`, `storeZipEntry`, and `mergeMetadata` create request-owned paths under the configured storage root.
`H2IntegrationTest` supplies a real Spring transaction interceptor, H2 repositories, and `onyx.storage.root=/tmp/onyx-kotlin-tests`.
Spring supplies `TransactionSynchronizationManager.isSynchronizationActive`, `registerSynchronization`, and `TransactionSynchronization.afterCompletion`.
</interfaces>
</context>

<execution_rules>
Do not make a RED test commit. Create one commit only after the focused test, regression class, and compile checks pass.
That commit must contain both changed Kotlin files and `02-01-SUMMARY.md`.
</execution_rules>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Remove request-owned files when their transaction rolls back</name>
  <files>_kotlin/backend/src/main/kotlin/com/onyx/foss/kotlin/service/FileStorageService.kt, _kotlin/backend/src/test/kotlin/com/onyx/foss/kotlin/api/AdminApiIntegrationTest.kt</files>
  <behavior>
    - D-14: A ZIP request that stores metadata and one normal entry before a later oversized entry fails returns HTTP 400.
    - D-14: After that response, the storage-root entry set equals its pre-request snapshot and `file_assets` has no request rows.
    - D-14: Rollback cleanup applies only to paths created by the current request; commit completion and pre-existing paths are unchanged.
    - ADMIN-01 through ADMIN-03 and PUBLIC-01 through PUBLIC-02: Existing behavior is regression-only and receives no product edits.
  </behavior>
  <action>
Add `failedZipUploadRemovesFilesCreatedByTheRolledBackRequest` beside the current ZIP limit test. Inject `OnyxProperties` into the existing test class and derive a normalized `Path` from `properties.storage.root`. In the test, take the storage-root filename set before the request. Build one ZIP in memory with `.onyx_metadata.json`, a small `one.txt`, and a final `large.txt` that writes 101 one-MiB zero blocks. Send it to the existing multipart upload endpoint. Assert HTTP 400, the same filename set after the response, and zero rows from `SELECT COUNT(*) FROM file_assets`.

Run the single method before changing production code. It must fail because files written before the oversized entry remain after database rollback. If it passes, stop and correct the test so it proves the reported leak.

In `FileStorageService`, keep both public transaction boundaries and API shapes unchanged. Add the Spring transaction synchronization imports and one private helper at the existing repository-save boundary. Route both `store` and `storeZipEntry` through it. The helper saves the `FileAssetEntity`, then registers an `afterCompletion` callback when synchronization is active. The callback calls `Files.deleteIfExists` only for `STATUS_ROLLED_BACK` and only for the new path passed by the caller. If repository save or callback registration throws, delete that path immediately and rethrow the original exception. Wrap the plain-file `Files.copy` with the same immediate delete-and-rethrow pattern already used by ZIP streaming.

Do not change `idsToRemove`, controllers, entities, APIs, configuration, successful-file lifetime, or any ADMIN-01 through ADMIN-03 and PUBLIC-01 through PUBLIC-02 flow. Do not add an interface, dependency, test class, or shared test helper. Per D-01, D-02, and D-03, do not edit root Python, root `web`, `_kotlin/web`, or `_kotlin/model-server`.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd _kotlin/backend; ./gradlew test --tests com.onyx.foss.kotlin.api.AdminApiIntegrationTest.failedZipUploadRemovesFilesCreatedByTheRolledBackRequest --no-daemon; ./gradlew test --tests com.onyx.foss.kotlin.api.AdminApiIntegrationTest --no-daemon; ./gradlew compileKotlin compileTestKotlin --warning-mode all --no-daemon'</automated>
    <fails_when>The command exits nonzero, the named rollback test is missing, a request-created path or row remains, an existing admin integration test regresses, or Kotlin compilation fails.</fails_when>
  </verify>
  <done>The new test fails before the service edit, then all three Gradle checks pass. The final diff contains only the two Kotlin files and the plan summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Multipart upload to filesystem and database transaction | Untrusted archive content creates physical files before database commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `FileStorageService` rollback cleanup | medium | mitigate | Register only request-created UUID paths at the shared save boundary and delete them only after rollback. |
| T-02-02 | Denial of Service | ZIP extraction failure | medium | mitigate | Preserve the 100 MiB limit and delete the current partial path immediately on failure. |
| T-02-03 | Tampering | Previously committed files | medium | mitigate | Do not derive cleanup targets from client IDs or `idsToRemove`; callbacks capture only newly created paths. |
</threat_model>

<verification>
1. Fail-first direction: the named test fails before the service change because the before/after storage sets differ.
2. D-14 proof: the named test passes after the shared cleanup is added and confirms both storage and database state.
3. Regression proof: the full `AdminApiIntegrationTest` class passes without changes outside the D-14 boundary.
4. Build proof: Kotlin production and test sources compile with all warnings enabled.
5. Scope proof: `git diff --name-only` lists only the two planned Kotlin files before the summary is created.
</verification>

<success_criteria>
- Failed ZIP uploads leave storage and `file_assets` unchanged from the request boundary.
- Committed uploads retain their files through the existing success tests.
- ADMIN-01 through ADMIN-03 and PUBLIC-01 through PUBLIC-02 keep their Phase 1 behavior and product code.
- No file outside `_kotlin/backend` changes, except Phase 2 planning artifacts.
- Execution creates one consolidated Phase 2 commit.
</success_criteria>

<source_audit>
| SOURCE | ID | Feature or requirement | Plan | Status | Notes |
|--------|----|------------------------|------|--------|-------|
| GOAL | — | Existing Web public-document management remains usable | 02-01 | COVERED | Existing contracts are regression-only; D-14 is the only product difference. |
| REQ | ADMIN-01 | Credential lifecycle and secret masking | 02-01 | COVERED | Existing integration behavior is unchanged and rerun with the admin class. |
| REQ | ADMIN-02 | Connector and CC Pair lifecycle | 02-01 | COVERED | Existing integration behavior is unchanged and rerun with the admin class. |
| REQ | ADMIN-03 | Document Set lifecycle and metadata sync | 02-01 | COVERED | Existing integration behavior is unchanged and rerun with the admin class. |
| REQ | ADMIN-04 | File upload, replace, remove, metadata, and rollback consistency | 02-01 | COVERED | The shared boundary closes D-14; existing success paths remain regression coverage. |
| REQ | PUBLIC-01 | Public indexing and empty ACLs | 02-01 | COVERED | Phase 1 current tests remain the evidence; no in-scope product path changes. |
| REQ | PUBLIC-02 | Permission-sync removal and V16 migration | 02-01 | COVERED | Phase 1 current tests remain the evidence; no in-scope product path changes. |
| RESEARCH | — | Shared transaction synchronization save boundary | 02-01 | COVERED | Uses Spring APIs already on the classpath. |
| RESEARCH | — | Immediate cleanup for copy or save failure | 02-01 | COVERED | Uses `Files.deleteIfExists` and rethrows. |
| RESEARCH | — | One request-level ZIP rollback test | 02-01 | COVERED | Tests metadata, completed entry, partial entry, DB rows, and storage entries. |
| CONTEXT | D-01 | Product edits only in `_kotlin/backend` | 02-01 | COVERED | Both changed product files are in that tree. |
| CONTEXT | D-02 | Root Python and root Web stay read-only | 02-01 | COVERED | Explicit scope guard. |
| CONTEXT | D-03 | No `_kotlin/web` edit unless required | 02-01 | COVERED | No Web edit is required. |
| CONTEXT | D-14 | Rollback removes only request-created files | 02-01 | COVERED | Implemented and tested at the shared save boundary. |
</source_audit>

<output>
Create `.planning/phases/02-public-document-management/02-01-SUMMARY.md` in the same consolidated commit when done.
</output>
